name: Apply Batch N Hidden Climate Swing

on:
  push:
    branches: [develop]
    paths: [".github/workflows/apply-batch-n-hidden-swing.yml"]

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: tools
    steps:
      - uses: actions/checkout@v7
        with:
          ref: develop
      - uses: actions/setup-python@v7
        with:
          python-version: "3.14"
      - run: python -m pip install "jsonschema>=4.23,<5" "PyYAML>=6,<7"
      - run: python tools/_apply_batch_n_hidden_swing.py
      - run: python -m py_compile tools/import_tuya_local_productless.py tests/test_productless_climate_hidden_swing.py
      - name: Run focused tests
        run: python -m unittest tests.test_productless_climate_hidden_swing -v
      - name: Run full catalog suite
        run: python -m unittest discover -s tests -v
      - name: Commit importer and tests
        shell: bash
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add tools/import_tuya_local_productless.py tests/test_productless_climate_hidden_swing.py
          if git diff --cached --quiet; then
            echo "Hidden swing support already applied"
            exit 0
          fi
          git commit -m "Support hidden productless Climate swing fallbacks"
          git push origin HEAD:develop
