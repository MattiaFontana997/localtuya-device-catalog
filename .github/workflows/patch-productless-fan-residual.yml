name: Patch residual productless fans

on:
  push:
    paths:
      - .github/workflows/patch-productless-fan-residual.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: develop
          fetch-depth: 0
      - uses: actions/setup-python@v7
        with:
          python-version: "3.14"
      - name: Apply patch
        run: python .github/scripts/patch_productless_fan_residual.py
      - name: Install dependencies
        run: python -m pip install "PyYAML>=6,<7" "jsonschema>=4.23,<5" pytest
      - name: Run tests
        run: python -m pytest -q tests/test_productless_fan_residual.py tests/test_import_tuya_local_productless.py
      - name: Commit importer support
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add tools/import_tuya_local_productless.py tests/test_productless_fan_residual.py
          git diff --cached --check
          git commit -m "Import residual no-switch and fallback fan semantics"
          git push origin HEAD:develop
