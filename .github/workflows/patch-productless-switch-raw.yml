name: Patch productless exact switches

on:
  push:
    paths:
      - .github/workflows/patch-productless-switch-raw.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: develop
          fetch-depth: 0
      - uses: actions/setup-python@v7
        with:
          python-version: "3.14"
      - name: Apply patch
        run: python .github/scripts/patch_productless_switch_raw.py
      - name: Install dependencies
        run: python -m pip install "PyYAML>=6,<7" "jsonschema>=4.23,<5" pytest
      - name: Run tests
        run: python -m pytest -q tests/test_productless_switch_raw.py tests/test_import_tuya_local_productless.py
      - name: Commit importer support
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add tools/import_tuya_local_productless.py tests/test_productless_switch_raw.py
          git diff --cached --check
          git commit -m "Import exact raw and masked switches"
          git push origin HEAD:develop
