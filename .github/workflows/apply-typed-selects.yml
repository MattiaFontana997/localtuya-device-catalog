name: Apply Typed Productless Selects

on:
  push:
    branches: [develop]
    paths:
      - 'tools/_apply_typed_selects.py'
      - '.github/workflows/apply-typed-selects.yml'
  workflow_dispatch:

permissions:
  contents: write

env:
  PYTHONPATH: tools

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: develop
      - uses: actions/setup-python@v7
        with:
          python-version: '3.14'
      - run: python -m pip install 'PyYAML>=6,<7' 'jsonschema>=4.23,<5'
      - name: Apply typed select patch
        run: python tools/_apply_typed_selects.py
      - name: Run productless tests
        run: python -m unittest tests.test_import_tuya_local_productless -v
      - name: Run full catalog suite
        run: python -m unittest discover -s tests -p 'test_*.py' -v
      - name: Validate current catalog
        run: python tools/validate_catalog.py catalog.json
      - name: Commit importer core
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add tools/import_tuya_local_productless.py tests/test_import_tuya_local_productless.py
          if git diff --cached --quiet; then
            echo 'Typed select support already current'
            exit 0
          fi
          git commit -m 'Support typed productless select mappings'
          git push origin HEAD:develop
